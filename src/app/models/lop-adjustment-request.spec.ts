import { LopAdjustmentRequest } from './lop-adjustment-request';

describe('LopAdjustmentRequest', () => {
  it('should create an instance', () => {
    expect(new LopAdjustmentRequest()).toBeTruthy();
  });
});
