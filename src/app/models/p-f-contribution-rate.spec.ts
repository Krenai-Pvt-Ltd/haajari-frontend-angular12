import { PFContributionRate } from './p-f-contribution-rate';

describe('PFContributionRate', () => {
  it('should create an instance', () => {
    expect(new PFContributionRate()).toBeTruthy();
  });
});
