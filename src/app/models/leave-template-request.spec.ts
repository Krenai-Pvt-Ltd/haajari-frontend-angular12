import { LeaveTemplateRequest } from './leave-template-request';

describe('LeaveTemplateRequest', () => {
  it('should create an instance', () => {
    expect(new LeaveTemplateRequest()).toBeTruthy();
  });
});
