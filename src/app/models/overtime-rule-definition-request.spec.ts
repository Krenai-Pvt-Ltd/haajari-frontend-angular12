import { OvertimeRuleDefinitionRequest } from './overtime-rule-definition-request';

describe('OvertimeRuleDefinitionRequest', () => {
  it('should create an instance', () => {
    expect(new OvertimeRuleDefinitionRequest()).toBeTruthy();
  });
});
