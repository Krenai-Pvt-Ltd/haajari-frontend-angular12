import { OrganizationWeekoffInformation } from './organization-weekoff-information';

describe('OrganizationWeekoffInformation', () => {
  it('should create an instance', () => {
    expect(new OrganizationWeekoffInformation()).toBeTruthy();
  });
});
