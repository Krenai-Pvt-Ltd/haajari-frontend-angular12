import { AttendanceRuleDefinitionRequest } from './attendance-rule-definition-request';

describe('AttendanceRuleDefinitionRequest', () => {
  it('should create an instance', () => {
    expect(new AttendanceRuleDefinitionRequest()).toBeTruthy();
  });
});
