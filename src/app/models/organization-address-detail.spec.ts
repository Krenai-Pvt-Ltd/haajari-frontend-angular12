import { OrganizationAddressDetail } from './organization-address-detail';

describe('OrganizationAddressDetail', () => {
  it('should create an instance', () => {
    expect(new OrganizationAddressDetail()).toBeTruthy();
  });
});
