import { OnboardingSidebarResponse } from './onboarding-sidebar-response';

describe('OnboardingSidebarResponse', () => {
  it('should create an instance', () => {
    expect(new OnboardingSidebarResponse()).toBeTruthy();
  });
});
