import { UserGuarantorRequest } from './user-guarantor-request';

describe('UserGuarantorRequest', () => {
  it('should create an instance', () => {
    expect(new UserGuarantorRequest()).toBeTruthy();
  });
});
