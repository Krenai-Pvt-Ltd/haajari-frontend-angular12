import { CustomSalaryDeductionRequest } from './custom-salary-deduction-request';

describe('CustomSalaryDeductionRequest', () => {
  it('should create an instance', () => {
    expect(new CustomSalaryDeductionRequest()).toBeTruthy();
  });
});
