import { SalaryTemplateComponentResponse } from './salary-template-component-response';

describe('SalaryTemplateComponentResponse', () => {
  it('should create an instance', () => {
    expect(new SalaryTemplateComponentResponse()).toBeTruthy();
  });
});
