import { LopSummaryRequest } from './lop-summary-request';

describe('LopSummaryRequest', () => {
  it('should create an instance', () => {
    expect(new LopSummaryRequest()).toBeTruthy();
  });
});
