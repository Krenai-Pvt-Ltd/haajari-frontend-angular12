import { StartDateAndEndDate } from './start-date-and-end-date';

describe('StartDateAndEndDate', () => {
  it('should create an instance', () => {
    expect(new StartDateAndEndDate()).toBeTruthy();
  });
});
