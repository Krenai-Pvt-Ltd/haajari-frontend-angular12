import { ModulesWithSubmodules } from './modules-with-submodules';

describe('ModulesWithSubmodules', () => {
  it('should create an instance', () => {
    expect(new ModulesWithSubmodules()).toBeTruthy();
  });
});
