import { UserInfoForPayrollReflection } from './user-info-for-payroll-reflection';

describe('UserInfoForPayrollReflection', () => {
  it('should create an instance', () => {
    expect(new UserInfoForPayrollReflection()).toBeTruthy();
  });
});
