import { SalaryChangeResponse } from './salary-change-response';

describe('SalaryChangeResponse', () => {
  it('should create an instance', () => {
    expect(new SalaryChangeResponse()).toBeTruthy();
  });
});
