import { UserBankDetailRequest } from './user-bank-detail-request';

describe('UserBankDetailRequest', () => {
  it('should create an instance', () => {
    expect(new UserBankDetailRequest()).toBeTruthy();
  });
});
