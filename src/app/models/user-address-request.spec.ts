import { UserAddressRequest } from './user-address-request';

describe('UserAddressRequest', () => {
  it('should create an instance', () => {
    expect(new UserAddressRequest()).toBeTruthy();
  });
});
