import { FullDaySalaryDeduction } from './full-day-salary-deduction';

describe('FullDaySalaryDeduction', () => {
  it('should create an instance', () => {
    expect(new FullDaySalaryDeduction()).toBeTruthy();
  });
});
