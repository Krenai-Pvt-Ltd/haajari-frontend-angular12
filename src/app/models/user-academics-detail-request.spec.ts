import { UserAcademicsDetailRequest } from './user-academics-detail-request';

describe('UserAcademicsDetailRequest', () => {
  it('should create an instance', () => {
    expect(new UserAcademicsDetailRequest()).toBeTruthy();
  });
});
