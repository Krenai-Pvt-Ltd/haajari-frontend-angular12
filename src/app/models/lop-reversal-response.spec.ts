import { LopReversalResponse } from './lop-reversal-response';

describe('LopReversalResponse', () => {
  it('should create an instance', () => {
    expect(new LopReversalResponse()).toBeTruthy();
  });
});
