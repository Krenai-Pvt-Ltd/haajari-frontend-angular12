import { UserAddressDetailsRequest } from './user-address-details-request';

describe('UserAddressDetailsRequest', () => {
  it('should create an instance', () => {
    expect(new UserAddressDetailsRequest()).toBeTruthy();
  });
});
