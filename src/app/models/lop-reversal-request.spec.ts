import { LopReversalRequest } from './lop-reversal-request';

describe('LopReversalRequest', () => {
  it('should create an instance', () => {
    expect(new LopReversalRequest()).toBeTruthy();
  });
});
