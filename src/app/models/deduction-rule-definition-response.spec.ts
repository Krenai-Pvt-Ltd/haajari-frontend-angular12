import { DeductionRuleDefinitionResponse } from './deduction-rule-definition-response';

describe('DeductionRuleDefinitionResponse', () => {
  it('should create an instance', () => {
    expect(new DeductionRuleDefinitionResponse()).toBeTruthy();
  });
});
