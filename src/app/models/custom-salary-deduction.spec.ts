import { CustomSalaryDeduction } from './custom-salary-deduction';

describe('CustomSalaryDeduction', () => {
  it('should create an instance', () => {
    expect(new CustomSalaryDeduction()).toBeTruthy();
  });
});
