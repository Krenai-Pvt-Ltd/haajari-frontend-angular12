import { SalaryChangeBonusResponse } from './salary-change-bonus-response';

describe('SalaryChangeBonusResponse', () => {
  it('should create an instance', () => {
    expect(new SalaryChangeBonusResponse()).toBeTruthy();
  });
});
