import { HalfDaySalaryDeduction } from './half-day-salary-deduction';

describe('HalfDaySalaryDeduction', () => {
  it('should create an instance', () => {
    expect(new HalfDaySalaryDeduction()).toBeTruthy();
  });
});
