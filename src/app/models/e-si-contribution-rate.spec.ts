import { ESIContributionRate } from './e-si-contribution-rate';

describe('ESIContributionRate', () => {
  it('should create an instance', () => {
    expect(new ESIContributionRate()).toBeTruthy();
  });
});
