import { UserPasswordRequest } from './user-password-request';

describe('UserPasswordRequest', () => {
  it('should create an instance', () => {
    expect(new UserPasswordRequest()).toBeTruthy();
  });
});
