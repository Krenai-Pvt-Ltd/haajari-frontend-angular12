import { LopReversalApplicationResponse } from './lop-reversal-application-response';

describe('LopReversalApplicationResponse', () => {
  it('should create an instance', () => {
    expect(new LopReversalApplicationResponse()).toBeTruthy();
  });
});
