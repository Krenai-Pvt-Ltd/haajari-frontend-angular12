import { UnusedLeaveAction } from './unused-leave-action';

describe('UnusedLeaveAction', () => {
  it('should create an instance', () => {
    expect(new UnusedLeaveAction()).toBeTruthy();
  });
});
