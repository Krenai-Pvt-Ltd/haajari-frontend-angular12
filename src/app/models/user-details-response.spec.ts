import { UserDetailsResponse } from './user-details-response';

describe('UserDetailsResponse', () => {
  it('should create an instance', () => {
    expect(new UserDetailsResponse()).toBeTruthy();
  });
});
