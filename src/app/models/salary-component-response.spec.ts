import { SalaryComponentResponse } from './salary-component-response';

describe('SalaryComponentResponse', () => {
  it('should create an instance', () => {
    expect(new SalaryComponentResponse()).toBeTruthy();
  });
});
