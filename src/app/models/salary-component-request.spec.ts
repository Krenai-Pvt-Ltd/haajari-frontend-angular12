import { SalaryComponentRequest } from './salary-component-request';

describe('SalaryComponentRequest', () => {
  it('should create an instance', () => {
    expect(new SalaryComponentRequest()).toBeTruthy();
  });
});
