import { LopReversalApplicationRequest } from './lop-reversal-application-request';

describe('LopReversalApplicationRequest', () => {
  it('should create an instance', () => {
    expect(new LopReversalApplicationRequest()).toBeTruthy();
  });
});
