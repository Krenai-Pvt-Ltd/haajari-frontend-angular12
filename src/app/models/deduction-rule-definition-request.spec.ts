import { DeductionRuleDefinitionRequest } from './deduction-rule-definition-request';

describe('DeductionRuleDefinitionRequest', () => {
  it('should create an instance', () => {
    expect(new DeductionRuleDefinitionRequest()).toBeTruthy();
  });
});
