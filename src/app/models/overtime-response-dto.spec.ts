import { OvertimeResponseDTO } from './overtime-response-dto';

describe('OvertimeResponseDTO', () => {
  it('should create an instance', () => {
    expect(new OvertimeResponseDTO()).toBeTruthy();
  });
});
