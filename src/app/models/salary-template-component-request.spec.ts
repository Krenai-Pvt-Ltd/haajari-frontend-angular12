import { SalaryTemplateComponentRequest } from './salary-template-component-request';

describe('SalaryTemplateComponentRequest', () => {
  it('should create an instance', () => {
    expect(new SalaryTemplateComponentRequest()).toBeTruthy();
  });
});
