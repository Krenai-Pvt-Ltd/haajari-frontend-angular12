import { UserDocumentsRequest } from './user-documents-request';

describe('UserDocumentsRequest', () => {
  it('should create an instance', () => {
    expect(new UserDocumentsRequest()).toBeTruthy();
  });
});
