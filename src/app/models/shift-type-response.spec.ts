import { ShiftTypeResponse } from './shift-type-response';

describe('ShiftTypeResponse', () => {
  it('should create an instance', () => {
    expect(new ShiftTypeResponse()).toBeTruthy();
  });
});
