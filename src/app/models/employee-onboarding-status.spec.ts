import { EmployeeOnboardingStatus } from './employee-onboarding-status';

describe('EmployeeOnboardingStatus', () => {
  it('should create an instance', () => {
    expect(new EmployeeOnboardingStatus()).toBeTruthy();
  });
});
