import { RoleAccessibilityType } from './role-accessibility-type';

describe('RoleAccessibilityType', () => {
  it('should create an instance', () => {
    expect(new RoleAccessibilityType()).toBeTruthy();
  });
});
