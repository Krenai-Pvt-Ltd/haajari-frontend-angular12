import { BASE_URL } from "src/environments/environment";

export const API_URLS={
      base_url : BASE_URL,
      leaves: '/leave',
      leavesCountReport: '/leave/report/monthly-count',
      teamsForFilter: '/team/abstract',
}